<%= form_for @recipe do |f| %>
  Title: <%= f.text_field :title %>
  <%= f.fields_for :ingredients do |rec| %>
  <%= rec.label :name %>
  <%= rec.text_field :name %><br>
  <%= rec.label :quantity %>
  <%= rec.text_field :quantity %><br>
  <% end %><br>
  <%= f.submit %>
<% end %>
